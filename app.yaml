---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-jvm-postgres
  labels:
    app: spring-jvm-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-jvm-postgres
  template:
    metadata:
      labels:
        app: spring-jvm-postgres
    spec:
      containers:
        - name: spring-jvm-postgres
          image: sarvangeeks/spring-jvm-postgres
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          env:
            - name: spring.datasource.url
              value: "jdbc:postgresql://postgresql.quarkuspoc-dev.svc.cluster.local:5432/sampledb"
      imagePullSecrets:
        - name: sarvandockerhub
---
apiVersion: v1
kind: Service
metadata:
  name: spring-jvm-postgres
  labels:
    app: spring-jvm-postgres
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: spring-jvm-postgres
  type: ClusterIP
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: spring-jvm-postgres
  namespace: quarkuspoc-dev
spec:
  path: /
  to:
    kind: Service
    name: spring-jvm-postgres
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None
